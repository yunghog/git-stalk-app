import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gitwatch.app',
  appName: 'Gitwatch',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
