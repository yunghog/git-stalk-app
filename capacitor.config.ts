import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gitstalk.app',
  appName: 'Gitstalk',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
