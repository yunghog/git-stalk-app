export enum SIGNUP_STEPS {
  EMAIL = 'email',
  DETAILS = 'details',
  VERIFICATION = 'verification',
  SUCCESS = 'success',
}
export enum SignupMethod {
  EMAIL = 'email',
  GITHUB = 'github',
}
export enum SnackbarType {
  ERROR = 'error',
  SUCCESS = 'success',
  INFO = 'info',
}
